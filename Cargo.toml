[package]
name = "connector"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "src/main/rust/lib.rs"

[profile.release]
strip = true
lto = true
panic = "abort"
codegen-units = 1

[dependencies]
jni = "0.22.0"
log = "0.4"

# Audio codec dependencies
fdk-aac-sys = { version = "0.4.0", optional = true }
mpg123-sys = { version = "0.6.0", optional = true }
ogg-sys = { version = "0.0.9", optional = true }
audiopus_sys = { version = "0.2.1", optional = true }
vorbis-sys = { version = "0.1.1", optional = true }
libsamplerate-sys = { version = "0.1.9", optional = true }

[features]
default = ["opus", "aac"]
opus = ["audiopus_sys"]
mp3 = ["mpg123-sys"]
aac = ["fdk-aac-sys"]
vorbis = ["vorbis-sys", "ogg-sys"]
samplerate = ["libsamplerate-sys"]
